describe('Menus Module', function() {
  it('should view Menus', function() {
    browser.get('/#/1651/menus');
    browser.ignoreSynchronization = true;
    browser.manage().timeouts().implicitlyWait(1000);

    expect(browser.getTitle()).toEqual('Bindo Dashboard - Menus');
  });
});