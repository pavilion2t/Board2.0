describe('Settings Module', function() {
  it('should view Settings', function() {
    browser.get('/#/1651/settings');
    browser.ignoreSynchronization = true;
    browser.manage().timeouts().implicitlyWait(1000);

    expect(browser.getTitle()).toEqual('Bindo Dashboard - Settings');
  });
});