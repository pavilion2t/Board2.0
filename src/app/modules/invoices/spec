describe('Inovice Module', function() {
  it('should view Invoices', function() {
    browser.get('/#/1651/invoices');
    browser.ignoreSynchronization = true;
    browser.manage().timeouts().implicitlyWait(1000);

    expect(browser.getTitle()).toEqual('Bindo Dashboard - Invoices');
  });
});