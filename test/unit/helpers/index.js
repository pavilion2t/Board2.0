import PermissionHelperSpec from './permissionHelper.spec';
import RoundHelperSpce from './roundHelper.spec';

export default function () {
    describe('Utils Helpers', () => {
        PermissionHelperSpec();
        RoundHelperSpce();
    });
}
